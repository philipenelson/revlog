import { Stack } from 'expo-router';

// Header (wordmark, offline indicator, gear icon -> /settings) is future
// work — see docs/specs/mobile-app/navigation.md "Garage stack header".
export default function GarageLayout() {
  return <Stack />;
}
