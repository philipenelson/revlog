// Shared React components for web (Next.js + Astro).
// Do NOT import from react-native — these are DOM components only.
// React Native apps import from @maintenance-log/ui-tokens only.

export {};
